import { LucideProps, User, LogOut, Settings, ChevronLeft, ChevronRight, Loader2, Sun, Moon, Mail, AlertCircle, Calendar } from "lucide-react";

export type IconProps = LucideProps;

export const Icons = {
  user: User,
  logOut: LogOut,
  settings: Settings,
  chevronLeft: ChevronLeft,
  chevronRight: ChevronRight,
  spinner: Loader2,
  sun: Sun,
  moon: Moon,
  mail: Mail,
  alert: AlertCircle,
  calendar: Calendar,
};

export const CalendarIcon = Calendar; 